package dmlc;
message TrainingSample {
	message Namespace {
		message Feature {
			required fixed64 id = 1;
			optional float value = 2 [default = 1.];
		}
		repeated Feature features [packed=true]; 
	}
	optional bool label = 1;
	repeated Namespace namespaces = 2 [packed=true];
}
