package dmlc.data;
message TrainingSample {
	message Namespace {
		repeated fixed64 features = 1 [packed=true]; 
		repeated float values = 2 [packed=true]; 
	}
	optional bool label = 1;
	repeated Namespace namespaces = 2;
}
